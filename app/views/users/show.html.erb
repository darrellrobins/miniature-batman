<h2><%= @user.name %></h2>
<p><%= @user.email %></p>
<div class="actions">
	<p><%= link_to "Edit", edit_user_path(@user) %></p>
	<p><%= link_to "Delete", user_path(@user), method: :delete, data: { confirm: 'Are you sure?' } %></p>
</div>
